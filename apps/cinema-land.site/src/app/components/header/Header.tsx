'use client';

import { LaptopCodeIcon, MoonIcon, SunIcon } from '../../icons';

export default function Header() {
  return (
    <div>
      <MoonIcon />
      <SunIcon />
      <LaptopCodeIcon />
    </div>
  );
}
